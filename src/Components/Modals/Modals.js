import * as React from "react";
import { ModalInput, ModalLoginForm } from "../../Components";
const Modals = props => {
  return (
    <div>
      <ModalInput />
      <ModalLoginForm />
    </div>
  );
};

export default Modals;
