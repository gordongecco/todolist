import * as React from "react";

import { ListInput, ListItems } from "../../Components";

const List = props => {
  return (
    <div>
      <ListInput />
      <ListItems />
    </div>
  );
};

export default List;
